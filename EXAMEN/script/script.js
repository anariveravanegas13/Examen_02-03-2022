let salario
let ch
const hora = 20000
const hExtra = 30000


function horasT(params) {
    hora = number(prompt('Ingrese la cantidad de las horas trabajadas'))
}
horasT()

function calcular(salario) {
    else if (horasT >=1 && horasT <=40) {
        salario = cantHora *horas
}